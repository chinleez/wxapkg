name: release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: wxapkg_macos_arm64
            android: false
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: wxapkg_macos_amd64
            android: false
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: wxapkg_windows_amd64.exe
            android: false
          - os: ubuntu-latest
            target: aarch64-linux-android
            artifact: wxapkg_android_arm64-v8a
            android: true
            cargo_target_env: AARCH64_LINUX_ANDROID
            linker: aarch64-linux-android23-clang
          - os: ubuntu-latest
            target: armv7-linux-androideabi
            artifact: wxapkg_android_armeabi-v7a
            android: true
            cargo_target_env: ARMV7_LINUX_ANDROIDEABI
            linker: armv7a-linux-androideabi23-clang
          - os: ubuntu-latest
            target: x86_64-linux-android
            artifact: wxapkg_android_x86_64
            android: true
            cargo_target_env: X86_64_LINUX_ANDROID
            linker: x86_64-linux-android23-clang
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Setup Android NDK
        if: ${{ matrix.android }}
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r26d
          add-to-path: false
      - uses: Swatinem/rust-cache@v2
      - name: Build
        if: ${{ ! matrix.android }}
        run: cargo build --release --target ${{ matrix.target }}
      - name: Build Android
        if: ${{ matrix.android }}
        shell: bash
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          export CARGO_TARGET_${{ matrix.cargo_target_env }}_LINKER="$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/${{ matrix.linker }}"
          cargo build --release --target ${{ matrix.target }}
      - name: Rename binary
        shell: bash
        run: |
          src=target/${{ matrix.target }}/release/wxapkg
          [[ "${{ matrix.target }}" == *windows* ]] && src="${src}.exe"
          cp "$src" "${{ matrix.artifact }}"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: wxapkg_*
          generate_release_notes: true
